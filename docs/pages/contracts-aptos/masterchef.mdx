# MasterChef

## 合约信息

**合约名称**: pancake::masterchef  
**合约地址**:

7968a225eba6c99f5f1070aeec1b405757dee939eabcfda43ba91588bf5fccf3::masterchef

**管理员多签地址**: eef8788f3eaa34b936d0bc5897fc40fa3782b1e663bf04edf2cd22fcd18329ff

[Aptos Explorer](https://explorer.aptoslabs.com/account/0x7968a225eba6c99f5f1070aeec1b405757dee939eabcfda43ba91588bf5fccf3/modules)

## 资源

### MasterChef

MasterChef 的元数据。

```rust
struct MasterChef has key {
    signer_cap: account::SignerCapability, // 资源账号的签名能力。
    admin: address,                       // 模块的管理员地址。
    upkeep_admin: address,                // 执行维护调用的账号。
    lp_to_pid: TableWithLength<string::String, u64>, // LP 代币类型信息及相应的池 ID。
    lps: vector<string::String>,          // 所有已添加 LP 代币类型信息数组。
    pool_info: vector<PoolInfo>,          // 池信息列表。
    total_regular_alloc_point: u64,       // 所有常规农场分配点总和。
    total_special_alloc_point: u64,       // 所有特殊农场分配点总和。
    cake_per_second: u64,                 // 所有农场池每秒的 CAKE 奖励。
    cake_rate_to_regular: u64,            // 常规农场可以获得的 CAKE 奖励百分比。
    cake_rate_to_special: u64,            // 特殊农场可以获得的 CAKE 奖励百分比。
    last_upkeep_timestamp: u64,           // 上次维护执行的时间戳。
    end_timestamp: u64                    // 农场可以获得 CAKE 奖励的最后时间，每次维护操作都会延长此时间戳。
}
```

| 名称                         | 类型                               | 描述                                                         |
| -------------------------- | -------------------------------- | ---------------------------------------------------------- |
| signer_cap                 | `account::SingerCapability`      | 资源账号的签名能力。                                           |
| admin                      | `address`                        | 模块的管理员地址。                                              |
| upkeep_admin               | `address`                        | 执行维护调用的账号。                                             |
| lp_to_pid                  | `table_with_length::TableWithLength` | LP 代币类型信息及相应的池 ID。                                        |
| lps                        | `vector`                         | 所有已添加 LP 代币类型信息数组。                                      |
| pool_info                  | `vector`                         | 池信息结构体的列表。                                              |
| total_regular_alloc_point  | `u64`                            | 所有常规农场分配点总和。                                            |
| total_special_alloc_point  | `u64`                            | 所有特殊农场分配点总和。                                            |
| cake_per_second            | `64`                             | 所有农场池每秒的 CAKE 奖励。                                       |
| cake_rate_to_regular       | `u64`                            | 常规农场可以获得的 CAKE 奖励百分比。                                      |
| cake_rate_to_special       | `64`                             | 特殊农场可以获得的 CAKE 奖励百分比。                                      |
| last_upkeep_timestamp      | `64`                             | 上次维护执行的时间戳。                                               |
| end_timestamp              | `64`                             | 农场可以获得 CAKE 奖励的最后时间，每次维护操作都会延长此时间戳。                          |

### PoolUserInfo

所有池信息。

```rust
struct PoolUserInfo has key {
    pid_to_user_info: TableWithLength<u64, UserInfo>, // 包含 user_info 的表，对应池 id。
    pids: vector<u64>,                                // 池 id 的列表。
}
```

| 名称                | 类型                                 | 描述                                             |
| ------------------- | ------------------------------------ | --------------------------------------------------- |
| pid\_to\_user\_info | `table_with_length::TableWithLength` | 包含 user_info 的表，对应池 id。                             |
| pids                | `vector`                             | 池 id 的列表。                                      |

### UserInfo

每个池中的用户信息。

```rust
struct UserInfo has store {
    amount: u128,       // 在池中抵押的代币总数量。
    reward_debt: u128   // 分发给用户的奖励总数量。
}
```

| 名称         | 类型   | 描述                                         |
| ------------ | ------ | --------------------------------------------------- |
| amount       | `u128` | 在池中抵押的代币总数量。                             |
| reward\_debt | `u128` | 分发给用户的奖励总数量。                             |

### PoolInfo

每个池的信息

```rust
struct PoolInfo has store {
    total_amount: u128,           // 抵押代币的总数量。
    acc_cake_per_share: u128,     // 池中的累计 CAKE 每股。
    last_reward_timestamp: u64,   // 最新的奖励分发时间。
    alloc_point: u64,             // 池分配点。
    is_regular: bool              // True 表示此池是常规农场，否则为特殊农场。
}
```

| 名称                    | 类型   | 描述                                                      |
| ----------------------- | ------ | ---------------------------------------------------------------- |
| total\_amount           | `u128` | 抵押代币的总数量。                                 |
| acc\_cake\_per\_share   | `u128` | 池中的累计 CAKE 每股。                     |
| last\_reward\_timestamp | `u64`  | 最新的奖励分发时间。                      |
| alloc\_point            | `u64`  | 池分配点。                                        |
| is\_regular             | `bool` | True 表示此池是常规农场，否则为特殊农场。 |

## 入口函数

### Deposit

将抵押代币存入池中。如果有奖励代币，也会分配给用户。

```rust
public entry fun deposit<CoinType>(
    sender: &signer, // 调用函数时的发送者签名器。
    amount: u64      // 将要存入的代币数量。
)
```

| 名称   | 类型     | 描述                                    |
| ------ | -------- | ---------------------------------------------- |
| sender | `signer` | 调用函数时的发送者签名器。                     |
| amount | `u64`    | 将要存入的代币数量。                          |

### Withdraw

从池中提取抵押代币。如果有奖励代币，也会分配给用户。

```rust
public entry fun withdraw<CoinType>(
    sender: &signer, // 调用函数时的发送者签名器。
    amount: u64      // 将要提取的代币数量。
)
```

| 名称   | 类型     | 描述                                    |
| ------ | -------- | ---------------------------------------------- |
| sender | `signer` | 调用函数时的发送者签名器。                     |
| amount | `u64`    | 将要提取的代币数量。                           |

### Emergency Withdraw

无论是否有奖励，从池中提取抵押代币。

```rust
public entry fun emergency_withdraw<CoinType>(sender: &signer) // 调用函数时的发送者签名器。
```

| 名称   | 类型     | 描述                                    |
| ------ | -------- | ---------------------------------------------- |
| sender | `signer` | 调用函数时的发送者签名器。                     |

### Mass Update Pools

更新所有池的信息。

```rust
public entry fun mass_update_pools()
```

### Update Pool

更新特定池的信息。

```rust
public entry fun update_pool(pid: u64) // 池的 id。
```

| 名称 | 类型  | 描述         |
| ---- | ----- | ------------------- |
| pid  | `u64` | 池的 id。         |

## 公共函数

### Pending Cake

用户的待领取 CAKE 奖励。

```rust
public fun pending_cake(
    pid: u64,       // 池的 id。
    user: address   // 存款者的地址。
): u64
```

#### 输入值

| 名称 | 类型      | 描述                   |
| ---- | --------- | ----------------------------- |
| pid  | `u64`     | 池的 id。                      |
| user | `address` | 存款者的地址。                   |

#### 返回值

| 类型  | 描述                      |
| ----- | -------------------------------- |
| `u64` | 待领取的 CAKE 奖励总数量。       |

### Pool Length

池的总数。

```rust
public fun pool_length():u64
```

#### 返回值

| 类型  | 描述                |
| ----- | -------------------------- |
| `u64` | 池的总数量。               |

## 审计

<a className="vocs_Anchor vocs_Link vocs_Link_accent_underlined" href="/PancakeSwap_aptos_masterchef_audit.pdf" target="_blank">
OtterSec 的 PancakeSwap Aptos MasterChef 安全审计
</a>